import { GasPresencePipe } from './gas-presence.pipe';

describe('GasPresencePipe', () => {
  it('create an instance', () => {
    const pipe = new GasPresencePipe();
    expect(pipe).toBeTruthy();
  });
});
